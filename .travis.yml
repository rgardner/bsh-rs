sudo: false
language: rust
cache: cargo
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
before_script:
  - rustup component add clippy-preview
  - rustup component add rustfmt-preview
script:
  - set -e
  - cargo fmt --all -- --check
  - cargo build --verbose
  - cargo clippy
  - cargo test --verbose
  - cargo test --release --verbose
  - set +e
