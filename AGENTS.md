# AGENTS.md

Run `cargo fmt` automatically after you have finished making Rust code changes; do not ask for approval to run it. Additionally, run the tests.

Before finalizing a large change, run `cargo clippy --fix` to fix any linter issues in the code.